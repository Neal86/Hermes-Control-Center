target-version = "py311"

[lint]
# CI blocks real correctness errors while allowing deliberate fail-closed / best-effort
# exception handling required by Windows UI Automation and Hermes compatibility layers.
select = ["E9", "F63", "F7", "F82"]
